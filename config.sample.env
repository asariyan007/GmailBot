# Telegram bot token
TELEGRAM_TOKEN=YOUR_TELEGRAM_BOT_TOKEN

# A random 32-byte key for Fernet (base64 urlsafe). Use: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=YOUR_FERNET_KEY

# Poll interval seconds to check emails
POLL_INTERVAL=5

# Optional: limit number of alias characters appended
ALIAS_RANDOM_LEN=6
